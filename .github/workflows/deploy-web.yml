name: Deploy Web

on:
  push:
    branches: [main]

jobs:
  test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

  deploy:
    name: Build & Deploy
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: linkup-web:latest
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=server:8080

      - name: Save image
        run: |
          docker save linkup-web | gzip > web.tar.gz

      - name: Copy to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: web.tar.gz
          target: /tmp/

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          timeout: 60s
          script_timeout: 10m
          script: |
            set -e
            cd /opt/linkup/web
            git pull origin main
            docker load -i /tmp/web.tar.gz
            cd /opt/linkup
            docker compose up -d web
            echo "Waiting for web (up to 60s)..."
            for i in $(seq 1 20); do
              if curl -sf -o /dev/null http://localhost:3000/; then
                echo "WEB OK after ${i}x3s"
                break
              fi
              sleep 3
            done
            curl -sf -o /dev/null http://localhost:3000/ || exit 1

      - name: Notify Telegram
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          format: html
          message: |
            ${{ job.status == 'success' && '✅' || '❌' }} <b>Web Deploy</b>
            Pusher: ${{ github.event.pusher.name }}
            Repo: <a href="${{ github.server_url }}/${{ github.repository }}">LinkUp_Web</a>
            Commit: ${{ github.event.head_commit.message || 'manual' }}
            Log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
